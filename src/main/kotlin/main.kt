/**
 * Note that any personal scratch work should go in the untracked [scratch] package.
 *
 * This is currently unused, but we may eventually make a CLI or something for ease of use.
 */
fun main() {
    println("Hello World")
}